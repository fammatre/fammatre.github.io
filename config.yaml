baseURL: 'http://garden.fammatre.org/'
languageCode: 'en-us'
title: 'Fammatre Elementary School Garden'
theme: 'PaperMod'

# PaperMod search config (search.md and fuse.js)
outputs:
    home:
        - HTML
        - RSS
        - JSON # is necessary

# Papermod theme options
params:
    # Set theme to light
    defaultTheme: light

    # Don't show the theme toggle
    disableThemeToggle: true


